version: 2
updates:
  # GitHub Actions — həftəlik
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels: ["chore"]

  # (Opsional) dil ekosistemləri üçün security-only nümunəsi:
  # - package-ecosystem: "npm"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #   open-pull-requests-limit: 5
  #   commit-message:
  #     prefix: "chore(deps)"
  #   labels: ["chore"]
  #   allow:
  #     - dependency-type: "direct"
